<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="../public/stylesheets/style.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block"
          href="/logout"
          >Logout</a
        >
      </div>
      <h3 class="text-3xl font-blod mb-4">
        <span class="font-light">Hello</span>, <%=user.name %>
      </h3>
      <h5 class="mb-5">You can create a new post.</h5>
      <form
        action="/post"
        method="post"
      >
        <textarea
          placeholder="What is your content"
          class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-lg"
          name="content"
        ></textarea>
        <input
          class="px-3 py-2 w-40 text-sm bg-blue-500 block rounded-md mt-2"
          type="submit"
          value="Create post"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach((post)=>{ %>
          <div
            class="post w-1/3 p-4 mb-2 rounded-md border-zinc-700 bg-zinc-800"
          >
            <h4 class="text-blue-500 mb-2"><%= post.name %></h4>
            <p class="text-sm tracking-tighter"><%= post.content %></p>
            <div class="btns flex justify-between mt-5">
              <a
                href=""
                class="text-blue-500 font-bold"
                >Like</a
              >
              <a
                class="text-zinc-500 font-bold"
                href="/edit/<%= post._id%>"
                >Edit</a
              >
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>

    <script src="../public/javascripts/index.js"></script>
  </body>
</html>
